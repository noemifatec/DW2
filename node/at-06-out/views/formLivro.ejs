<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= livro ? "Editar Livro" : "Novo Livro" %></title>
</head>
<body>
  <h1><%= livro ? "Editar Livro" : "Novo Livro" %></h1>

  <form method="POST" action="<%= livro ? '/catalogo/editar' : '/catalogo/novo' %>">
    <% if (livro) { %>
      <input type="hidden" name="id" value="<%= livro.id %>">
    <% } %>

    <label>Título:</label>
    <input type="text" name="titulo" value="<%= livro ? livro.titulo : '' %>" required><br>

    <label>Autor:</label>
    <input type="text" name="autor" value="<%= livro ? livro.autor : '' %>" required><br>

    <button type="submit">Salvar</button>
  </form>

  <a href="/catalogo">⬅ Voltar ao Catálogo</a>
</body>
</html>
